= Προγραμματισμένες εργασίες

Το σύστημα εκτελεί αυτόματα συγκεκριμένες εργασίες σε προκαθορισμένα χρονικά διαστήματα.

== Σύνοψη εργασιών

[options="header",cols="2a,1,2m,3"]
|===
|Εργασία|Συχνότητα|Cron expression|Java class

|xref:#abandoned-rentals-job[Εγκαταλελειμμένες ενοικιάσεις]
|Κάθε 3 λεπτά
|0 1/3 * ? * *
|AbandonedRentalsJob

|xref:#finsish-rentals-job[Ολοκλήρωση ενοικιάσεων]
|Κάθε 10 λεπτά
|0 1/10 * ? * *
|FinishRentalsJob

|xref:#rental-followup-emails-job[Follow-up emails ενοικιάσεων]
|1 φορά/ημέρα (08:00)
|0 0 8 ? * *
|RentalFollowupEmailsJob

|Καθαρισμός Cache Google Place
|1 φορά/μήνα
|0 0 1 1 1/1 ? *
|_(Jmix Google Place plugin)_

|xref:#audit-log-cleanup-job[Καθαρισμός Audit Logs]
|1 φορά/ημέρα (02:00)
|0 0 2 ? * *
|AuditLogCleanupJob

|xref:#notification-cleanup-job[Καθαρισμός Notifications]
|1 φορά/ημέρα (03:00)
|0 0 3 ? * *
|NotificationCleanupJob

|Αποστολή Email
|Κάθε 2 λεπτά
|* 0/2 * * * ?
|_(Jmix Email Sending plugin)_

|Καθαρισμός Email
|Κάθε 2 ημέρες
|0 0 0 1/2 * ?
|_(Jmix Email Sending plugin)_
|===

[[abandoned-rentals-job]]
== Εγκαταλελειμμένες ενοικιάσεις

Η εργασία αυτή αναλαμβάνει να ακυρώσει τις ενοικιάσεις και να αποδεσμεύσει τα οχήματα (σε επίπεδο πινακίδας), που έχουν δεσμευτεί κατά τη διαδικασία ενοικίασης, εφόσον έχει παρέλθει ο σχετικός χρόνος xref:admin/settings-system.adoc#rental-reserved-period[Περίοδος δέσμευσης ενοικίασης (λεπτά)].

Έχει ρυθμιστεί να εκτελείται αυτόματα κάθε τρία λεπτά.

[source,properties]
----
nmcr.abandoned-rentals-cron = 0 1/3 * ? * *
----

Αν δε θέλουμε να ενεργοποιηθεί αυτόματα η εργασία, αλλά να τη ρυθμίσουμε μέσα από το UI, αλλάζουμε το property

.application.properties
[source,properties]
----
nmcr.use-default-abandoned-rentals-quartz-configuration = false
----

.Java class for the job
----
gr.netmechanics.carrental.job.AbandonedRentalsJob
----

[[finsish-rentals-job]]
== Ολοκλήρωση ενοικιάσεων

Η εργασία αυτή αναλαμβάνει να χαρακτηρίσει τις ενοικιάσεις σαν ολοκληρωμένες και να αποδεσμεύσει τα οχήματα (σε επίπεδο πινακίδας), που έχουν δεσμευτεί. Ο έλεγχος γίνεται με βάση την ημερομηνία παράδοσης και τηv xref:admin/settings-system.adoc#rental-finished-offset[Χρονική μετατόπιση ολοκλήρωσης ενοικίασης (λεπτά)] που έχει οριστεί.

Έχει ρυθμιστεί να εκτελείται αυτόματα κάθε δέκα λεπτά.

[source,properties]
----
nmcr.finish-rentals-cron = 0 1/10 * ? * *
----

Αν δε θέλουμε να ενεργοποιηθεί αυτόματα η εργασία, αλλά να τη ρυθμίσουμε μέσα από το UI, αλλάζουμε το property

.application.properties
[source,properties]
----
nmcr.use-default-finish-rentals-quartz-configuration = false
----

.Java class for the job
----
gr.netmechanics.carrental.job.FinishRentalsJob
----

[[rental-followup-emails-job]]
== Follow-up emails ενοικιάσεων

Η εργασία αυτή αναλαμβάνει την αυτόματη αποστολή δύο ειδών follow-up email:

* reminder πριν από την παραλαβή,
* thank-you μετά την ολοκλήρωση της ενοικίασης.

Τα follow-up emails είναι απενεργοποιημένα από default και ενεργοποιούνται ρητά από τις xref:admin/settings-system.adoc#rental-reminder-enabled[Ρυθμίσεις Συστήματος].

Έχει ρυθμιστεί να εκτελείται μία φορά την ημέρα.

[source,properties]
----
nmcr.rental-followup-emails-cron = 0 0 8 ? * *
----

Η λογική βασίζεται σε ημερολογιακές ημέρες και όχι σε rolling παράθυρο ωρών. Για παράδειγμα, αν το reminder είναι ορισμένο στις `3` ημέρες, ο job ελέγχει όλες τις κρατήσεις που έχουν pickup στην αντίστοιχη ημερολογιακή ημέρα.

Για να μην αποσταλεί δεύτερο email για την ίδια κράτηση, το σύστημα κρατά εσωτερική πληροφορία ότι το email έχει ήδη μπει στην ουρά αποστολής.

.Java class for the job
----
gr.netmechanics.carrental.job.RentalFollowupEmailsJob
----

== Καθαρισμός Cache Google Place

Η εργασία αυτή προέρχεται από το πρόσθετο https://www.jmix.io/marketplace/google-place/[Google Place,window=_blank] του Jmix. Αναλαμβάνει τον καθαρισμό της cache από δεδομένα που υπάρχουν από τη χρήση της υπηρεσίας Google Place.

Έχει ρυθμιστεί να εκτελείται αυτόματα μια φορά το μήνα.

[source,properties]
----
jmix.gplace.cleaning-cache-cron = 0 0 1 1 1/1 ? *
----

Αν δε θέλουμε να ενεργοποιηθεί αυτόματα η εργασία, αλλά να τη ρυθμίσουμε μέσα από το UI, αλλάζουμε το property

.application.properties
[source,properties]
----
jmix.gplace.use-default-cleaning-cache-quartz-configuration = false
----

== Αποστολή Email

Η εργασία αυτή προέρχεται από το πρόσθετο https://www.jmix.io/marketplace/email-sending/["Email Sending",window=_blank] του Jmix. Αναλαμβάνει την αποστολή των email που βρίσκονται στην ουρά.

Έχει ρυθμιστεί να εκτελείται αυτόματα κάθε δύο λεπτά.

[source,properties]
----
jmix.email.email-sending-cron = * 0/2 * * * ?
----

NOTE: Για περισσότερες πληροφορίες δείτε την τεκμηρίωση της εργασίας https://docs.jmix.io/jmix/email/configuration.html#email-sending-scheduler["Email Sending Scheduler",window=_blank].

== Καθαρισμός Email

Η εργασία αυτή προέρχεται από το πρόσθετο https://www.jmix.io/marketplace/email-sending/["Email Sending",window=_blank] του Jmix. Αναλαμβάνει τον καθαρισμό των αποθηκευμένων email.

Έχει ρυθμιστεί να εκτελείται κάθε δύο ημέρες.

[source,properties]
----
jmix.email.email-cleaning-cron = 0 0 0 1/2 * ?
----

NOTE: Για περισσότερες πληροφορίες δείτε την τεκμηρίωση της εργασίας https://docs.jmix.io/jmix/email/configuration.html#email-cleaning-scheduler["Email Cleaning Scheduler",window=_blank].

[[audit-log-cleanup-job]]
== Καθαρισμός Audit Logs

Η εργασία αυτή διαγράφει rental audit log records παλαιότερα από το configured retention period.

Έχει ρυθμιστεί να εκτελείται αυτόματα καθημερινά στις 2:00 πμ.

[source,properties]
----
nmcr.use-default-audit-log-cleanup-cron-quartz-configuration = true
nmcr.audit-log-cleanup-cron = 0 0 2 ? * *
----

Αν δε θέλουμε να ενεργοποιηθεί αυτόματα η εργασία, αλλά να τη ρυθμίσουμε μέσα από το UI, αλλάζουμε το property

.application.properties
[source,properties]
----
nmcr.use-default-audit-log-cleanup-quartz-configuration = false
----

IMPORTANT: Το retention period (προεπιλογή: 90 ημέρες) ρυθμίζεται από τις xref:admin/settings-system.adoc[Ρυθμίσεις Συστήματος] → Κανόνες Ενοικίασης → Audit log retention (days).

.Java class for the job
----
gr.netmechanics.carrental.job.AuditLogCleanupJob
----

Δείτε επίσης την τεκμηρίωση xref:admin/audit-logs.adoc[Rental Audit Logging].

[[notification-cleanup-job]]
== Καθαρισμός Notifications

Η εργασία αυτή διαγράφει in-app notifications (`ntf_InAppNotification`) παλαιότερες από το configured retention period για αποφυγή συσσώρευσης δεδομένων.

Έχει ρυθμιστεί να εκτελείται αυτόματα καθημερινά στις 3:00 πμ.

[source,properties]
----
nmcr.use-default-notification-cleanup-quartz-configuration = true
nmcr.notification-cleanup-cron = 0 0 3 ? * *
----

Αν δε θέλουμε να ενεργοποιηθεί αυτόματα η εργασία, αλλά να τη ρυθμίσουμε μέσα από το UI, αλλάζουμε το property

.application.properties
[source,properties]
----
nmcr.use-default-notification-cleanup-quartz-configuration = false
----

IMPORTANT: Το retention period (προεπιλογή: 90 ημέρες) ρυθμίζεται από τις xref:admin/settings-system.adoc[Ρυθμίσεις Συστήματος] → Κανόνες Ενοικίασης → Notification retention (days).

.Java class for the job
----
gr.netmechanics.carrental.job.NotificationCleanupJob
----
